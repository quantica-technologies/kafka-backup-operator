---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: restores.backup.kafka.io
spec:
  group: backup.kafka.io
  names:
    kind: Restore
    listKind: RestoreList
    plural: restores
    shortNames:
    - kr
    singular: restore
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.topicsRestored
      name: Topics
      type: integer
    - jsonPath: .status.totalMessages
      name: Messages
      type: integer
    - jsonPath: .status.completionTime
      name: Duration
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Restore is the Schema for the restores API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: RestoreSpec defines the desired state of Restore
            properties:
              backupName:
                description: |-
                  BackupName references a specific backup to restore
                  If empty, restores the latest backup
                type: string
              backupTimestamp:
                description: BackupTimestamp specifies a point-in-time to restore
                  to
                format: date-time
                type: string
              createTopics:
                default: true
                description: CreateTopics specifies whether to create topics if they
                  don't exist
                type: boolean
              overwriteExisting:
                description: OverwriteExisting specifies whether to overwrite existing
                  data
                type: boolean
              partitionFilter:
                additionalProperties:
                  items:
                    format: int32
                    type: integer
                  type: array
                description: PartitionFilter allows restoring specific partitions
                  only
                type: object
              resources:
                description: Resources defines resource requirements for restore pods
                properties:
                  limits:
                    description: Limits describes the maximum resources allowed
                    properties:
                      cpu:
                        description: CPU resource quantity
                        type: string
                      memory:
                        description: Memory resource quantity
                        type: string
                    type: object
                  requests:
                    description: Requests describes the minimum resources required
                    properties:
                      cpu:
                        description: CPU resource quantity
                        type: string
                      memory:
                        description: Memory resource quantity
                        type: string
                    type: object
                type: object
              source:
                description: Source is the name of the storage configuration to restore
                  from
                type: string
              target:
                description: Target is the name of the Kafka cluster to restore to
                type: string
              topicMapping:
                additionalProperties:
                  type: string
                description: TopicMapping allows renaming topics during restore
                type: object
              topicSelectors:
                description: |-
                  TopicSelectors define which topics to restore
                  If empty, restores all topics from backup
                properties:
                  matchers:
                    description: Matchers define topic selection rules
                    items:
                      description: TopicMatcher defines a single topic matching rule
                      properties:
                        glob:
                          description: Glob pattern for topic names
                          type: string
                        name:
                          description: Name can be exact name or glob pattern
                          type: string
                        regex:
                          description: Regex pattern for topic names
                          type: string
                      type: object
                    type: array
                required:
                - matchers
                type: object
              workers:
                default: 1
                description: Workers is the number of parallel restore workers
                minimum: 1
                type: integer
            required:
            - source
            - target
            type: object
          status:
            description: RestoreStatus defines the observed state of Restore
            properties:
              completionTime:
                description: CompletionTime is when the restore completed
                format: date-time
                type: string
              conditions:
                description: Conditions represent the latest available observations
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              errors:
                description: Errors contains any errors encountered during restore
                items:
                  type: string
                type: array
              observedGeneration:
                description: ObservedGeneration reflects the generation observed by
                  the controller
                format: int64
                type: integer
              phase:
                description: Phase represents the current phase of the restore
                enum:
                - Pending
                - Validating
                - Running
                - Completed
                - Failed
                - PartiallyCompleted
                type: string
              progress:
                description: Progress shows restore progress per topic
                items:
                  description: TopicProgress represents restore progress for a topic
                  properties:
                    messagesRestored:
                      description: MessagesRestored is the number of messages restored
                      format: int64
                      type: integer
                    partitionsCompleted:
                      description: PartitionsCompleted is the number of completed
                        partitions
                      type: integer
                    partitionsTotal:
                      description: PartitionsTotal is the total number of partitions
                      type: integer
                    status:
                      description: Status is the current status of the topic restore
                      type: string
                    topicName:
                      description: TopicName is the name of the topic
                      type: string
                  required:
                  - messagesRestored
                  - partitionsCompleted
                  - partitionsTotal
                  - status
                  - topicName
                  type: object
                type: array
              startTime:
                description: StartTime is when the restore started
                format: date-time
                type: string
              topicsRestored:
                description: TopicsRestored is the number of topics restored
                type: integer
              totalBytes:
                description: TotalBytes is the total size of restored data
                format: int64
                type: integer
              totalMessages:
                description: TotalMessages is the total number of messages restored
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
