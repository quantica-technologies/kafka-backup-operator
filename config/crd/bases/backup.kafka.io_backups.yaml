---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: backups.backup.kafka.io
spec:
  group: backup.kafka.io
  names:
    kind: Backup
    listKind: BackupList
    plural: backups
    shortNames:
    - kb
    singular: backup
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.topicsBackedUp
      name: Topics
      type: integer
    - jsonPath: .status.totalMessages
      name: Messages
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Backup is the Schema for the backups API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BackupSpec defines the desired state of Backup
            properties:
              batchSize:
                default: 1000
                description: BatchSize is the number of messages per batch
                type: integer
              compression:
                default: true
                description: Compression enables GZIP compression
                type: boolean
              flushInterval:
                default: 30s
                description: FlushInterval is the time between flushes
                type: string
              resources:
                description: Resources defines resource requirements for backup pods
                properties:
                  limits:
                    description: Limits describes the maximum resources allowed
                    properties:
                      cpu:
                        description: CPU resource quantity
                        type: string
                      memory:
                        description: Memory resource quantity
                        type: string
                    type: object
                  requests:
                    description: Requests describes the minimum resources required
                    properties:
                      cpu:
                        description: CPU resource quantity
                        type: string
                      memory:
                        description: Memory resource quantity
                        type: string
                    type: object
                type: object
              schedule:
                description: |-
                  Schedule for periodic backups (cron format)
                  If not specified, runs continuously
                type: string
              sink:
                description: Sink is the name of the storage configuration to use
                type: string
              source:
                description: Source is the name of the Kafka cluster to backup
                type: string
              suspend:
                description: Suspend pauses the backup job
                type: boolean
              topicSelectors:
                description: TopicSelectors define which topics to backup
                properties:
                  matchers:
                    description: Matchers define topic selection rules
                    items:
                      description: TopicMatcher defines a single topic matching rule
                      properties:
                        glob:
                          description: Glob pattern for topic names
                          type: string
                        name:
                          description: Name can be exact name or glob pattern
                          type: string
                        regex:
                          description: Regex pattern for topic names
                          type: string
                      type: object
                    type: array
                required:
                - matchers
                type: object
              workGroup:
                description: WorkGroup defines worker distribution settings
                properties:
                  seed:
                    description: Seed for topic distribution hashing
                    type: integer
                  workers:
                    description: Workers is the number of worker pods
                    minimum: 1
                    type: integer
                required:
                - workers
                type: object
            required:
            - sink
            - source
            - topicSelectors
            type: object
          status:
            description: BackupStatus defines the observed state of Backup
            properties:
              conditions:
                description: Conditions represent the latest available observations
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              lastBackupTime:
                description: LastBackupTime is the timestamp of the last successful
                  backup
                format: date-time
                type: string
              observedGeneration:
                description: ObservedGeneration reflects the generation observed by
                  the controller
                format: int64
                type: integer
              phase:
                description: Phase represents the current phase of the backup
                enum:
                - Pending
                - Running
                - Completed
                - Failed
                - Suspended
                type: string
              topicsBackedUp:
                description: TopicsBackedUp is the number of topics being backed up
                type: integer
              totalBytes:
                description: TotalBytes is the total size of backed up data
                format: int64
                type: integer
              totalMessages:
                description: TotalMessages is the total number of messages backed
                  up
                format: int64
                type: integer
              workerStatus:
                description: WorkerStatus contains status of each worker
                items:
                  description: WorkerStatus represents the status of a single worker
                  properties:
                    lastHeartbeat:
                      description: LastHeartbeat from the worker
                      format: date-time
                      type: string
                    messagesProcessed:
                      description: MessagesProcessed by this worker
                      format: int64
                      type: integer
                    status:
                      description: Status of the worker pod
                      type: string
                    topics:
                      description: Topics assigned to this worker
                      items:
                        type: string
                      type: array
                    workerId:
                      description: WorkerID is the identifier of the worker
                      type: integer
                  required:
                  - workerId
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
