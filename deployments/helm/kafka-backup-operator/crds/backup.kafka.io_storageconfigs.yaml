---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: storageconfigs.backup.kafka.io
spec:
  group: backup.kafka.io
  names:
    kind: StorageConfig
    listKind: StorageConfigList
    plural: storageconfigs
    shortNames:
    - sc
    singular: storageconfig
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: StorageConfig defines a storage backend configuration
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: StorageConfigSpec defines storage backend configuration
            properties:
              azure:
                description: Azure configuration
                properties:
                  accountKey:
                    description: AccountKey for Azure storage
                    type: string
                  accountName:
                    description: AccountName for Azure storage
                    type: string
                  secretRef:
                    description: SecretRef references a secret containing credentials
                    properties:
                      key:
                        description: Key in the secret
                        type: string
                      name:
                        description: Name of the secret
                        type: string
                      namespace:
                        description: Namespace of the secret
                        type: string
                    required:
                    - name
                    type: object
                type: object
              gcs:
                description: GCS configuration
                properties:
                  credentialsFile:
                    description: CredentialsFile path to GCS credentials
                    type: string
                  secretRef:
                    description: SecretRef references a secret containing credentials
                    properties:
                      key:
                        description: Key in the secret
                        type: string
                      name:
                        description: Name of the secret
                        type: string
                      namespace:
                        description: Namespace of the secret
                        type: string
                    required:
                    - name
                    type: object
                type: object
              path:
                description: Path is the bucket name or local path
                type: string
              prefix:
                description: Prefix for all stored objects
                type: string
              region:
                description: Region for cloud storage
                type: string
              s3:
                description: S3 configuration
                properties:
                  accessKeyId:
                    description: AccessKeyID for S3 authentication
                    type: string
                  endpoint:
                    description: Endpoint for S3-compatible storage
                    type: string
                  secretAccessKey:
                    description: SecretAccessKey for S3 authentication
                    type: string
                  secretRef:
                    description: SecretRef references a secret containing credentials
                    properties:
                      key:
                        description: Key in the secret
                        type: string
                      name:
                        description: Name of the secret
                        type: string
                      namespace:
                        description: Namespace of the secret
                        type: string
                    required:
                    - name
                    type: object
                  usePathStyle:
                    description: UsePathStyle enables path-style URLs
                    type: boolean
                type: object
              type:
                description: Type of storage backend
                enum:
                - s3
                - azure
                - gcs
                - local
                type: string
            required:
            - path
            - type
            type: object
        type: object
    served: true
    storage: true
